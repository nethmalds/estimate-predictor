You are a Minimum Viable Estimation Dataset (MVED) validator for construction projects.
Check whether the description below provides all required fields to generate a construction cost estimate.

Required fields for ALL project types:
- building_type      (residential | commercial | industrial | mixed_use)
- floors
- built_up_area
- finish_level       (basic | standard | high_end)
- structural_system  (framed | load_bearing | hybrid)
- roof_type
- location           (Sri Lankan district or city)

Additional required fields for RESIDENTIAL projects only:
- bedrooms
- bathrooms

Fields to check when implied or mentioned in the description:
- soil_condition       (check when site works, excavation, or deep foundation are implied)
- drainage_type        (check when sanitary, drainage, or sewage scope is implied)
- external_works_scope (check when paving, landscaping, boundary wall, or external works are implied)

For every missing or ambiguous field: add its name to missing_fields and write one specific, friendly clarification question.

Description:
${description}

Return JSON only with this exact schema:
{
  "requirements_satisfied": <true | false>,
  "missing_fields": ["<field_name>", ...],
  "questions": ["<question text matching each missing field in the same order>"]
}

Return JSON only. No markdown, no explanation.
